{% for inmueble in inmuebles %}
    
    {# {% for usuario in {{ inmueble.usuarios }} %}
	  {{ usuario.dni }}
    {% endfor %} #}
    {% set arrays = inmueble.usuarios %}
    {% for array in arrays %}
    {{ array.nombre }}
    {% endfor %}
    
{% endfor %} 
